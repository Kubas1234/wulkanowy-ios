name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - name: Select Xcode
      run: sudo xcode-select -switch /Applications/Xcode_12.4.app
    - name: Xcode version
      run: /usr/bin/xcodebuild -version
    - name: Build archive
      run: |
        xcodebuild -sdk iphoneos -project wulkanowy.xcodeproj \
          -configuration Release -scheme wulkanowy \
          -derivedDataPath DerivedData \
          -archivePath DerivedData/Archive/Wulkanowy archive
    - name: Export Archive
      run: |
        xcodebuild -exportArchive \
          -archivePath DerivedData/Archive/Wulkanowy.xcarchive \
          -exportOptionsPlist provisioning/App-Store.plist \
          -exportPath DerivedData/ipa

